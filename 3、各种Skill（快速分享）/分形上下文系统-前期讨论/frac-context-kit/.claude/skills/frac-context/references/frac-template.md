# .frac.md — <relative/path>

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：YYYY-MM-DD HH:MM
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

用 3-8 行说明这个目录的职责、边界、在上级中的位置。

不要写泛泛描述；只写对后续编辑有帮助的信息。

## 2. 直属文件

- `file_a.ext`：做什么；对外暴露什么；修改时要注意什么。
- `file_b.ext`：做什么；谁会读取它；和哪些直属文件有关。

如果直属文件很多，按功能分组，但不要写成长篇清单。

## 3. 子目录导航

- `child_a/`：见 `child_a/.frac.md`；一句话说明它负责什么。
- `child_b/`：见 `child_b/.frac.md`；一句话说明它负责什么。

父级只总结子级 `.frac.md` 的结论，不直接读取孙目录源码或资料。

## 4. 本层约束 / 约定 / 禁忌

- 写本目录内真实存在的命名、架构、数据流、依赖、权限、同步、格式约束。
- 不写“保持简洁”“注意安全”“提升可维护性”这类空话。

## 5. 什么时候需要下钻

- 修改 X 时，先读 `child_a/.frac.md`。
- 改 Y 时，必须同时看 `file_b.ext` 和 `child_b/.frac.md`。
- 只在确实必要时给出下钻建议。
