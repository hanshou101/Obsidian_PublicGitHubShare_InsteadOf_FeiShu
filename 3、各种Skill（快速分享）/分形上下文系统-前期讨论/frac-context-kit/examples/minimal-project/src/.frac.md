# .frac.md — src

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-07-06 00:00
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

`src/` 存放应用源码。本层只提供源码模块导航，不直接总结深层业务文件。

## 2. 直属文件

- `main.ts`：应用入口，负责装配主要模块。

## 3. 子目录导航

- `auth/`：认证相关逻辑，见 `auth/.frac.md`。

## 4. 本层约束 / 约定 / 禁忌

- 新增顶级业务模块时，应在本文件的“子目录导航”中补充一句话导航。

## 5. 什么时候需要下钻

- 修改登录、会话、认证 provider 时，读 `auth/.frac.md`。
