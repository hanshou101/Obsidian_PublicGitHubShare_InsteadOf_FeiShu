# .frac.md — .

> 生成规则：本文件只总结当前目录的直属文件，以及直属子目录的 `.frac.md`。
> 最近更新：2026-07-06 00:00
> 输入范围：direct-files + child-fracs only

## 1. 本目录是什么

这是一个最小代码项目示例，用于展示 `.frac.md` 如何沿目录树递归组织上下文。

## 2. 直属文件

- `README.md`：项目入口说明。

## 3. 子目录导航

- `src/`：应用源码入口，见 `src/.frac.md`。
- `docs/`：项目说明资料，见 `docs/.frac.md`。

## 4. 本层约束 / 约定 / 禁忌

- 根级 `.frac.md` 不直接总结 `src/auth/login.ts` 等深层文件。
- 根级只依赖 `src/.frac.md` 和 `docs/.frac.md`。

## 5. 什么时候需要下钻

- 修改登录逻辑时，读 `src/.frac.md`，再读 `src/auth/.frac.md`。
- 修改文档结构时，读 `docs/.frac.md`。
